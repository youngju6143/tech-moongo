// ── Shelf geometry constants (sw = shelf width is computed at runtime) ───────
export const SH = 1.2     // height per shelf unit
export const SD = 0.75    // depth (앞표지 max aspect ~0.67 × max height ~0.95 + 여유)
export const PT = 0.06    // panel thickness
export const ST = 0.08    // side thickness
export const GAP = 0.012  // gap between books
